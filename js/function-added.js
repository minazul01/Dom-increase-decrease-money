function inputIdField(id){
   const add = document.getElementById(id).value;
    const change = parseFloat(add);
    return change
}
function  elementIdfieldId(id){
    const add = document.getElementById(id);
    const change = parseFloat(add).innerText;
    return change;
}
